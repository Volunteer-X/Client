import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const UserFeeds = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center' }}>
      <Text style={{ color: '#FFF' }}>UserFeeds</Text>
    </View>
  );
};

export default UserFeeds;

const styles = StyleSheet.create({});
